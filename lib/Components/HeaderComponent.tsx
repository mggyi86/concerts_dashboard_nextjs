'use client'
import { Layout } from 'antd';
import React from 'react'

const { Header } = Layout;

const HeaderComponent = () => {
  return (
    <Header style={{ padding: 0, background: "rgba(255,255,255,0)" }} />
  )
}

export default HeaderComponent